# SPDX-FileCopyrightText: 2026 Damián Búho <damian.buho@proton.me>
#
# SPDX-License-Identifier: MIT
#
# Generated by pf-ci from org.projectfile.ci — DO NOT EDIT.
# Regenerate with: pf-ci generate --target gha
# A freshness gate (pf-ci generate --check) runs in CI so this file cannot
# silently drift from the source DAG.
---
name: published
"on":
  push: {}
  pull_request: {}
  workflow_dispatch:
    inputs:
      B19_COLOR:
        type: string
        default: "1"
      B19_FETCH_DOCKER_CACHE:
        type: string
        default: "Y"
      B19_FETCH_LOCAL_CACHE:
        type: string
        default: "Y"
      B19_LOCALES:
        type: string
      B19_OFFGRID_MODE:
        type: string
        default: "N"
      B19_UBUNTU_MIRROR_AMD64:
        type: string
      B19_UBUNTU_MIRROR_ARM64:
        type: string
      B19_UBUNTU_MIRROR_RISCV64:
        type: string
      B19_UBUNTU_SERIES:
        type: string
        default: "resolute"
      B19_VERBOSITY:
        type: string
        default: "info"
      DESTINATION_DOCKER_REGISTRY:
        type: string
      LANG:
        type: string
      M6E_APT_CACHE_HOST:
        type: string
      M6E_APT_CACHE_PORT:
        type: string
      M6E_BUILD_DEBUG:
        type: string
      M6E_NEAR_CACHE_HOST:
        type: string
      M6E_NEAR_CACHE_PORT:
        type: string
      SOURCE_DOCKER_REGISTRY:
        type: string
permissions:
  contents: read
env:
  M6E_TAG: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
  M6E_RUN_SCOPE: ${{ github.run_id }}-${{ github.run_attempt }}
jobs:
  artifact-ready:
    runs-on: ubuntu-latest
    needs: ["built"]
    steps:
      - run: echo artifact-ready
  built:
    runs-on: ubuntu-latest
    needs: ["source-is-ready"]
    steps:
      - run: echo built
  container-is-ready:
    runs-on: ubuntu-latest
    needs: ["images-are-current"]
    steps:
      - run: echo container-is-ready
  container-is-verified:
    runs-on: ${{ matrix.M6E_RUNNER }}
    needs: ["container-ready-to-test"]
    strategy:
      matrix:
        B19_UBUNTU_SERIES: ["resolute", "noble"]
        M6E_ARCH: ["amd64"]
        include:
          - M6E_ARCH: "amd64"
            M6E_RUNNER: "ubuntu-latest"
    env:
      B19_UBUNTU_SERIES: ${{ matrix.B19_UBUNTU_SERIES }}
      M6E_ARCH: ${{ matrix.M6E_ARCH }}
      M6E_IMAGE_FULLNAME: b19/ubuntu/${{ matrix.B19_UBUNTU_SERIES }}:${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}-${{ github.run_id }}-${{ matrix.M6E_ARCH }}
      M6E_COMPOSE_PROJECT_NAME: ci-b19-ubuntu-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}-${{ github.run_attempt }}
      M6E_CONTAINER_INSTANCE: ci-b19-ubuntu-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: image-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}
      # The archive enters the runner's SHARED image store, where a sibling job's reap
      # can delete a layer record mid-copy — podman then buries the real cause under a
      # four-transport probe that claims the tar matches no supported format. The retry
      # for that class lives in the pinned action, not inline (Law 2).
      - uses: projectfile-org/actions/container-load@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          archive: image-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}.tar
      - name: secrets-provision
        uses: projectfile-org/actions/secrets-provision@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          declarations: "{\"b19.test.secret\":{\"value\":\"hello-from-shell-d\"}}"
          dir: .secrets
          default-image: ${{ vars.D9T_MISC_TOOLS_IMAGE || format('{0}/d9t/misc-tools:{1}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', env.M6E_TAG) }}
          image: b19/ubuntu/${{ matrix.B19_UBUNTU_SERIES }}:${{ env.M6E_TAG }}-${{ github.run_id }}-${{ matrix.M6E_ARCH }}
      - name: dc-pull
        run: M6E_COMPOSE_FILE=.compose/pipeline.yaml .makefile/container/scripts/compose-pull.sh
      - name: dc-up-d
        run: timeout --kill-after=10s "${M6E_COMPOSE_UP_TIMEOUT:-300}s" docker compose --project-name "$M6E_COMPOSE_PROJECT_NAME" --file .compose/pipeline.yaml up --detach --renew-anon-volumes --no-build </dev/null
      - name: container-test
        uses: projectfile-org/actions/container-exec@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          container: ${{ env.M6E_CONTAINER_INSTANCE }}
          run: test.d
      - name: dc-down
        if: ${{ always() }}
        run: docker compose --project-name "$M6E_COMPOSE_PROJECT_NAME" --file .compose/pipeline.yaml down --volumes </dev/null
      # Reap the run-scoped image tag AND the live stack's network. Both are unique per
      # run (the tag carries M6E_RUN_SCOPE, the network the run-scoped project name), so
      # without this the runner's docker store gains one image per run forever AND a
      # failed/timed-out `up` can orphan the network (compose reports the removal error
      # WITHOUT failing). RmiImage and RmNetwork are always PAIRED (single assignment
      # site), so one docker-cleanup step reaps both — the imperative reaps + their
      # `inspect` guard live in the pinned action, not inline (Law 2).
      - if: ${{ always() }}
        uses: projectfile-org/actions/docker-cleanup@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: b19/ubuntu/${{ matrix.B19_UBUNTU_SERIES }}:${{ env.M6E_TAG }}-${{ github.run_id }}-${{ matrix.M6E_ARCH }}
          network: ci-b19-ubuntu-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ env.M6E_RUN_SCOPE }}-network
  container-ready-to-test:
    runs-on: ubuntu-latest
    needs: ["container-is-ready"]
    steps:
      - run: echo container-ready-to-test
  documentation-is-ready:
    runs-on: ubuntu-latest
    needs: ["documentation-passes-lint"]
    steps:
      - run: echo documentation-is-ready
  documentation-passes-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: auto-markdownlint
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_JS_TOOLS_IMAGE || format('{0}/d9t/js-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-markdownlint
      - name: auto-textlint
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_JS_TOOLS_IMAGE || format('{0}/d9t/js-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-textlint
  fetched:
    runs-on: ubuntu-latest
    steps:
      - run: echo fetched
  image-is-built:
    runs-on: ${{ matrix.M6E_RUNNER }}
    needs: ["source-is-ready"]
    concurrency:
      group: build-${{ github.repository }}-${{ toJSON(matrix) }}
      cancel-in-progress: false
    strategy:
      max-parallel: 2
      matrix:
        B19_UBUNTU_SERIES: ["resolute", "noble"]
        M6E_ARCH: ["amd64", "arm64", "riscv64"]
        include:
          - M6E_ARCH: "amd64"
            M6E_RUNNER: "ubuntu-latest"
          - M6E_ARCH: "arm64"
            M6E_RUNNER: "ubuntu-24.04-arm"
          - M6E_ARCH: "riscv64"
            M6E_RUNNER: "ubuntu-26.04-riscv"
    env:
      B19_UBUNTU_SERIES: ${{ matrix.B19_UBUNTU_SERIES }}
      M6E_ARCH: ${{ matrix.M6E_ARCH }}
      M6E_NAMESPACE: b19
      M6E_PROJECT: ubuntu
      B19_COLOR: ${{ inputs.B19_COLOR || vars.B19_COLOR || '1' }}
      B19_FD_IMAGE: ${{ vars.B19_FD_IMAGE || format('{0}/b19/fd:{1}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', vars.BASE_IMAGE_DEFAULT_VERSION || 'latest') }}
      B19_FETCH_DOCKER_CACHE: ${{ inputs.B19_FETCH_DOCKER_CACHE || vars.B19_FETCH_DOCKER_CACHE || 'Y' }}
      B19_FETCH_LOCAL_CACHE: ${{ inputs.B19_FETCH_LOCAL_CACHE || vars.B19_FETCH_LOCAL_CACHE || 'Y' }}
      B19_LOCALES: ${{ inputs.B19_LOCALES || vars.B19_LOCALES }}
      B19_MINIJINJA_IMAGE: ${{ vars.B19_MINIJINJA_IMAGE || format('{0}/b19/minijinja:{1}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', vars.BASE_IMAGE_DEFAULT_VERSION || 'latest') }}
      B19_OFFGRID_MODE: ${{ inputs.B19_OFFGRID_MODE || vars.B19_OFFGRID_MODE || 'N' }}
      B19_UBUNTU_MIRROR_AMD64: ${{ inputs.B19_UBUNTU_MIRROR_AMD64 || vars.B19_UBUNTU_MIRROR_AMD64 }}
      B19_UBUNTU_MIRROR_ARM64: ${{ inputs.B19_UBUNTU_MIRROR_ARM64 || vars.B19_UBUNTU_MIRROR_ARM64 }}
      B19_UBUNTU_MIRROR_RISCV64: ${{ inputs.B19_UBUNTU_MIRROR_RISCV64 || vars.B19_UBUNTU_MIRROR_RISCV64 }}
      B19_VERBOSITY: ${{ inputs.B19_VERBOSITY || vars.B19_VERBOSITY || 'info' }}
      DESTINATION_DOCKER_REGISTRY: ${{ inputs.DESTINATION_DOCKER_REGISTRY || vars.DESTINATION_DOCKER_REGISTRY }}
      LANG: ${{ inputs.LANG || vars.LANG }}
      M6E_APT_CACHE_HOST: ${{ inputs.M6E_APT_CACHE_HOST || vars.M6E_APT_CACHE_HOST }}
      M6E_APT_CACHE_PORT: ${{ inputs.M6E_APT_CACHE_PORT || vars.M6E_APT_CACHE_PORT }}
      M6E_BUILD_DEBUG: ${{ inputs.M6E_BUILD_DEBUG || vars.M6E_BUILD_DEBUG }}
      M6E_NEAR_CACHE_HOST: ${{ inputs.M6E_NEAR_CACHE_HOST || vars.M6E_NEAR_CACHE_HOST }}
      M6E_NEAR_CACHE_PORT: ${{ inputs.M6E_NEAR_CACHE_PORT || vars.M6E_NEAR_CACHE_PORT }}
      SOURCE_DOCKER_REGISTRY: ${{ inputs.SOURCE_DOCKER_REGISTRY || vars.SOURCE_DOCKER_REGISTRY }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: container-build
        uses: projectfile-org/actions/container-build/buildx@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          artifact-name: image-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}
          image: b19/ubuntu/${{ matrix.B19_UBUNTU_SERIES }}:${{ env.M6E_TAG }}-${{ github.run_id }}-${{ matrix.M6E_ARCH }}
          build-args: |-
            B19_COLOR=${{ inputs.B19_COLOR || vars.B19_COLOR || '1' }}
            B19_FD_IMAGE=${{ vars.B19_FD_IMAGE || format('{0}/b19/fd:{1}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', env.M6E_TAG) }}
            B19_FETCH_DOCKER_CACHE=${{ inputs.B19_FETCH_DOCKER_CACHE || vars.B19_FETCH_DOCKER_CACHE || 'Y' }}
            B19_FETCH_LOCAL_CACHE=${{ inputs.B19_FETCH_LOCAL_CACHE || vars.B19_FETCH_LOCAL_CACHE || 'Y' }}
            B19_LOCALES=${{ inputs.B19_LOCALES || vars.B19_LOCALES }}
            B19_MINIJINJA_IMAGE=${{ vars.B19_MINIJINJA_IMAGE || format('{0}/b19/minijinja:{1}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', env.M6E_TAG) }}
            B19_OFFGRID_MODE=${{ inputs.B19_OFFGRID_MODE || vars.B19_OFFGRID_MODE || 'N' }}
            B19_UBUNTU_MIRROR_AMD64=${{ inputs.B19_UBUNTU_MIRROR_AMD64 || vars.B19_UBUNTU_MIRROR_AMD64 }}
            B19_UBUNTU_MIRROR_ARM64=${{ inputs.B19_UBUNTU_MIRROR_ARM64 || vars.B19_UBUNTU_MIRROR_ARM64 }}
            B19_UBUNTU_MIRROR_RISCV64=${{ inputs.B19_UBUNTU_MIRROR_RISCV64 || vars.B19_UBUNTU_MIRROR_RISCV64 }}
            B19_UBUNTU_SERIES=${{ matrix.B19_UBUNTU_SERIES }}
            B19_VERBOSITY=${{ inputs.B19_VERBOSITY || vars.B19_VERBOSITY || 'info' }}
            DESTINATION_DOCKER_REGISTRY=${{ inputs.DESTINATION_DOCKER_REGISTRY || vars.DESTINATION_DOCKER_REGISTRY }}
            LANG=${{ inputs.LANG || vars.LANG }}
            M6E_APT_CACHE_HOST=${{ inputs.M6E_APT_CACHE_HOST || vars.M6E_APT_CACHE_HOST }}
            M6E_APT_CACHE_PORT=${{ inputs.M6E_APT_CACHE_PORT || vars.M6E_APT_CACHE_PORT }}
            M6E_ARCH=${{ matrix.M6E_ARCH }}
            M6E_BUILD_DEBUG=${{ inputs.M6E_BUILD_DEBUG || vars.M6E_BUILD_DEBUG }}
            M6E_NAMESPACE=b19
            M6E_NEAR_CACHE_HOST=${{ inputs.M6E_NEAR_CACHE_HOST || vars.M6E_NEAR_CACHE_HOST }}
            M6E_NEAR_CACHE_PORT=${{ inputs.M6E_NEAR_CACHE_PORT || vars.M6E_NEAR_CACHE_PORT }}
            M6E_PROJECT=ubuntu
            SOURCE_DOCKER_REGISTRY=${{ inputs.SOURCE_DOCKER_REGISTRY || vars.SOURCE_DOCKER_REGISTRY }}
          file-args: B19_UBUNTU_HASH=.container/foundation/deps/ubuntu/${{ matrix.B19_UBUNTU_SERIES }}.sha256.deps
          mounts: fetch=/fetch
          pf-cli-image: ${{ vars.PF_CLI_IMAGE || format('{0}/projectfile/cli:{1}', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho', env.M6E_TAG) }}
          # This CELL's architecture, off the arch axis derived from the declared set.
          # One arch per call: the single-image archive is what every consumer reads.
          platform: ${{ matrix.M6E_ARCH }}
  image-is-ready:
    runs-on: ubuntu-latest
    needs: ["container-is-verified", "image-is-secure", "image-is-valid"]
    steps:
      - run: echo image-is-ready
  image-is-secure:
    runs-on: ${{ matrix.M6E_RUNNER }}
    needs: ["image-is-built"]
    strategy:
      matrix:
        B19_UBUNTU_SERIES: ["resolute", "noble"]
        M6E_ARCH: ["amd64", "arm64", "riscv64"]
        include:
          - M6E_ARCH: "amd64"
            M6E_RUNNER: "ubuntu-latest"
          - M6E_ARCH: "arm64"
            M6E_RUNNER: "ubuntu-24.04-arm"
          - M6E_ARCH: "riscv64"
            M6E_RUNNER: "ubuntu-26.04-riscv"
    env:
      B19_UBUNTU_SERIES: ${{ matrix.B19_UBUNTU_SERIES }}
      M6E_ARCH: ${{ matrix.M6E_ARCH }}
      M6E_IMAGE_ARCHIVE: image-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}.tar
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: image-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}
      - name: restore grype-db
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ${{ runner.temp }}/ci-cache/grype-db
          key: ci-cache-grype-db-
          restore-keys: |
            ci-cache-grype-db-
      - name: grype-scan-tar
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-grype image
          env: |-
            B19_UBUNTU_SERIES=${{ matrix.B19_UBUNTU_SERIES }}
            M6E_ARCH=${{ matrix.M6E_ARCH }}
            M6E_IMAGE_ARCHIVE=image-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}.tar
          mounts: ${{ runner.temp }}/ci-cache/grype-db:/app/.cache/grype:ro
      - name: restore trivy-db
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ${{ runner.temp }}/ci-cache/trivy-db
          key: ci-cache-trivy-db-
          restore-keys: |
            ci-cache-trivy-db-
      - name: trivy-scan-tar
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-trivy image
          env: |-
            B19_UBUNTU_SERIES=${{ matrix.B19_UBUNTU_SERIES }}
            M6E_ARCH=${{ matrix.M6E_ARCH }}
            M6E_IMAGE_ARCHIVE=image-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}.tar
            TRIVY_CACHE_BACKEND
          mounts: ${{ runner.temp }}/ci-cache/trivy-db:/app/.cache/trivy:ro
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: ${{ always() }}
        with:
          name: reports-image-is-secure-${{ matrix.B19_UBUNTU_SERIES }}-${{ matrix.M6E_ARCH }}-${{ github.run_id }}
          path: reports/
          if-no-files-found: warn
          overwrite: true
  image-is-valid:
    runs-on: ubuntu-latest
    needs: ["image-is-built"]
    steps:
      - run: echo image-is-valid
  images-are-current:
    runs-on: ubuntu-latest
    needs: ["image-is-built", "secrets-ready"]
    steps:
      - run: echo images-are-current
  projectfile-is-synced:
    runs-on: ubuntu-latest
    needs: ["projectfile-is-valid"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: pf-bridge-check
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.PF_BRIDGE_IMAGE || format('{0}/projectfile/bridge', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: pf-bridge check
  projectfile-is-valid:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: pf-validate
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.PF_CLI_IMAGE || format('{0}/projectfile/cli', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: pf-cli validate
  publish-image:
    runs-on: ubuntu-latest
    needs: ["image-is-built", "ready-to-publish"]
    if: (startsWith(github.ref, 'refs/heads/') && github.ref != 'refs/heads/main') || (startsWith(github.ref, 'refs/tags/'))
    concurrency:
      group: publish-${{ github.repository }}-${{ toJSON(matrix) }}
      cancel-in-progress: false
    strategy:
      matrix:
        B19_UBUNTU_SERIES: ["resolute", "noble"]
        M6E_PUBLISH_SINK: ["ghcr"]
    env:
      B19_UBUNTU_SERIES: ${{ matrix.B19_UBUNTU_SERIES }}
      DOCKERHUB_REGISTRY_PASSWORD: ${{ secrets.DOCKERHUB_REGISTRY_PASSWORD }}
      DOCKERHUB_REGISTRY_USERNAME: ${{ secrets.DOCKERHUB_REGISTRY_USERNAME }}
      REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
      REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: image-${{ matrix.B19_UBUNTU_SERIES }}-amd64-${{ github.run_id }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: image-${{ matrix.B19_UBUNTU_SERIES }}-arm64-${{ github.run_id }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: image-${{ matrix.B19_UBUNTU_SERIES }}-riscv64-${{ github.run_id }}
      - name: oci-push
        # This cell's destination gate — on the STEP because a job `if:` may not read
        # the matrix context, and a forge rejects the whole file when one does.
        if: vars.CI_PUBLISH_SINKS == '' || contains(format(',{0},', vars.CI_PUBLISH_SINKS), format(',{0},', matrix.M6E_PUBLISH_SINK))
        uses: projectfile-org/actions/oci-push@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          # One tar per DECLARED architecture, published together as a manifest list at
          # each cascade tag. Per-arch TAGS are deliberately not published: they are
          # scaffolding, and a registry that cannot delete a tag keeps them forever.
          archives: |
            amd64 image-${{ matrix.B19_UBUNTU_SERIES }}-amd64-${{ github.run_id }}
            arm64 image-${{ matrix.B19_UBUNTU_SERIES }}-arm64-${{ github.run_id }}
            riscv64 image-${{ matrix.B19_UBUNTU_SERIES }}-riscv64-${{ github.run_id }}
          image: b19/ubuntu/${{ matrix.B19_UBUNTU_SERIES }}:${{ env.M6E_TAG }}-${{ github.run_id }}
          version: ${{ github.ref_name }}
          # The branch this run is on, empty on a tag. Non-empty collapses the cascade to
          # one `latest-<branch>` tag on the primary sink, so a branch build is testable
          # without a tag and without moving anything a consumer floats on.
          preview: ${{ github.ref_type == 'branch' && github.ref_name || '' }}
          # OUTPUT_REGISTRY: the forge-dependent PUSH target (kiota.ch / ghcr.io / …),
          # always threaded — empty => Docker Hub. NOT the per-namespace INPUT registries
          # (B19_DOCKER_REGISTRY & friends answer "where base images come FROM"); this
          # answers "where THIS project publishes TO". Set per forge (org var = default,
          # repo var = project-wise override). Ignored when `refs` below is present.
          registry: ${{ vars.OUTPUT_REGISTRY }}
          # Every destination this forge's route declares, each reference composed by
          # the sink that declared it — so one archive lands under whatever path grammar
          # each registry wants. Credentials key on the SINK name.
          refs: |
            ghcr ghcr.io/damian-buho/b19/ubuntu/${{ matrix.B19_UBUNTU_SERIES }}:latest
          # This CELL's destination, off the publish axis: the job runs one cell per
          # place the archive goes, so a registry refusing a push fails only its own.
          sink: ${{ matrix.M6E_PUBLISH_SINK }}
  published:
    runs-on: ubuntu-latest
    needs: ["publish-image", "ready-to-publish"]
    steps:
      - run: echo published
  ready-to-publish:
    runs-on: ubuntu-latest
    needs: ["artifact-ready", "documentation-is-ready", "image-is-ready", "source-is-compliant", "source-is-secure"]
    steps:
      - run: echo ready-to-publish
  secrets-ready:
    runs-on: ubuntu-latest
    needs: ["image-is-built"]
    steps:
      - run: echo secrets-ready
  source-is-compliant:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: i18n-check
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_MISC_TOOLS_IMAGE || format('{0}/d9t/misc-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-i18n check
      - name: reuse-lint
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_PYTHON_TOOLS_IMAGE || format('{0}/d9t/python-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: reuse lint
  source-is-ready:
    runs-on: ubuntu-latest
    needs: ["fetched", "projectfile-is-synced", "source-is-tested", "source-passes-lint"]
    steps:
      - run: echo source-is-ready
  source-is-secure:
    runs-on: ubuntu-latest
    env:
      GRYPE_DB_AUTO_UPDATE: true
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: restore grype-db
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ${{ runner.temp }}/ci-cache/grype-db
          key: ci-cache-grype-db-
          restore-keys: |
            ci-cache-grype-db-
      - name: grype-scan-source-gha
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-grype
          env: |-
            GRYPE_DB_AUTO_UPDATE=true
          mounts: ${{ runner.temp }}/ci-cache/grype-db:/app/.cache/grype:rw
      - name: save grype-db
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ${{ runner.temp }}/ci-cache/grype-db
          key: ci-cache-grype-db-${{ github.run_id }}
      - name: restore trivy-db
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ${{ runner.temp }}/ci-cache/trivy-db
          key: ci-cache-trivy-db-
          restore-keys: |
            ci-cache-trivy-db-
      - name: trivy-scan-source
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_GO_TOOLS_IMAGE || format('{0}/d9t/go-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-trivy fs
          env: |-
            TRIVY_CACHE_BACKEND
          mounts: ${{ runner.temp }}/ci-cache/trivy-db:/app/.cache/trivy:ro
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: ${{ always() }}
        with:
          name: reports-source-is-secure-${{ github.run_id }}
          path: reports/
          if-no-files-found: warn
          overwrite: true
  source-is-tested:
    runs-on: ubuntu-latest
    steps:
      - run: echo source-is-tested
  source-passes-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          persist-credentials: false
      - name: auto-cffr-validate
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_R_TOOLS_IMAGE || format('{0}/d9t/r-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-cffr-validate
      - name: auto-jsonlint
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_JS_TOOLS_IMAGE || format('{0}/d9t/js-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-jsonlint
      - name: auto-shellcheck
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_MISC_TOOLS_IMAGE || format('{0}/d9t/misc-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-shellcheck
      - name: auto-yamllint
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_PYTHON_TOOLS_IMAGE || format('{0}/d9t/python-tools', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: auto-yamllint
      - name: hadolint
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.HADOLINT_IMAGE || 'hadolint/hadolint' }}
          version: v2.15.1
          pull: missing
          run: hadolint Dockerfile
      - name: ignorelint-check
        uses: projectfile-org/actions/run-tool@afa4711d0aa234f2a57295baf3aa3aad3ad99ba3 # v1
        with:
          image: ${{ vars.D9T_IGNORELINT_IMAGE || format('{0}/d9t/ignorelint', vars.SOURCE_DOCKER_REGISTRY || 'ghcr.io/damian-buho') }}
          version: ${{ env.M6E_TAG }}
          run: ignorelint
